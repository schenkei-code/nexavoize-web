Lege hier die aktuelle APK ab:

    nexavoize-latest.apk

Sobald die Datei existiert, funktioniert der Download-Button aus dem Hero automatisch.
Bis dahin liefert Vercel eine 404 zurück (intentional — kein Fake-Download).
